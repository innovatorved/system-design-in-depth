window.MODULE_CONTENT = window.MODULE_CONTENT || {};
window.MODULE_CONTENT["company-cases"] = {
  "case-instagram-early-architecture": {
    "title": "Case: Instagram's first architecture",
    "video": {
      "youtubeId": "hnpzNAPiC0E",
      "title": "Scaling Instagram Infrastructure",
      "channel": "InfoQ"
    },
    "content": "<div class=\"lesson-content\">\n      <h2>Case: Instagram's First Architecture</h2>\n      <p>Instagram's early scaling work is legendary because it was achieved by an incredibly small team. It separated three jobs that appear together on a phone: running application code, retrieving records, and delivering image bytes. Its engineers' 2011 posts and Mike Krieger's 2012 talk let us follow those choices.</p>\n      \n      <p>When Instagram launched in 2010, they grew from 0 to 1 million users in just a few months, with only a handful of engineers. They couldn't afford complex microservices; they needed extreme simplicity and reliable off-the-shelf tools.</p>\n      \n      <h3>The Core Stack: Keep It Simple</h3>\n      <p>The original architecture was built entirely on Amazon Web Services (AWS) using technologies that were boring and predictable:</p>\n      <ul>\n        <li><strong>OS:</strong> Ubuntu Linux</li>\n        <li><strong>Load Balancing:</strong> Amazon Elastic Load Balancer (ELB) routing requests to Nginx.</li>\n        <li><strong>Application Server:</strong> Django running on Python with Gunicorn.</li>\n        <li><strong>Data Store:</strong> PostgreSQL for user data and metadata, Redis for feeds and sessions.</li>\n        <li><strong>Storage:</strong> Amazon S3 for photos, CloudFront for CDN.</li>\n      </ul>\n      \n      <h3>Scaling the Database: Sharding IDs</h3>\n      <p>The most fascinating part of early Instagram was how they scaled PostgreSQL. As they grew rapidly, a single database could no longer hold all the data or handle the read/write volume.</p>\n      \n      <p>They needed to partition (shard) the data across multiple database instances. But this introduced a problem: how do you generate unique IDs for photos when you don't have a single central database keeping track of the auto-incrementing primary key?</p>\n      \n      <p>Their solution was brilliant and simple. They created a custom ID generation scheme using PL/pgSQL directly inside the database shards. The 64-bit ID consisted of:</p>\n      <ul>\n        <li>41 bits for time in milliseconds (about 69.7 years from the chosen custom epoch).</li>\n        <li>13 bits that represent the logical shard ID.</li>\n        <li>10 bits that represent an auto-incrementing sequence, local to each schema (modulo 1024).</li>\n      </ul>\n      \n      <p>This allowed the application to route a request directly to the correct shard just by inspecting the photo ID, without needing a separate lookup table to find where the photo lived.</p>\n      \n      <div class=\"mermaid\">\nflowchart TD\n  Client[\"Mobile App\"] --> ELB[\"Load Balancer\"]\n  ELB --> App1[\"Django Server\"]\n  ELB --> App2[\"Django Server\"]\n  App1 --> S3[\"Amazon S3\"]\n  App1 --> Redis[\"Redis Clusters\"]\n  App1 --> Postgres[(\"Postgres Shards\")]\n      </div>\n      \n      <h3>Storing the Feed in Redis</h3>\n      <p>To serve the home feed quickly, Instagram heavily utilized Redis. Whenever someone posted a photo, the photo ID was pushed into the Redis lists of all their followers. Because Redis keeps all data in memory, pulling a user's feed was instantaneous.</p>\n      <p>To save memory, they didn't store full JSON objects in Redis, only the photo IDs. The application would fetch the IDs from Redis, and then do a quick <code>SELECT IN</code> query against PostgreSQL to get the photo metadata (like caption and author).</p>\n    </div>",
    "keyTakeaways": [
      "Keep the architecture boring and simple; avoid complex, unproven technologies when scaling rapidly.",
      "Separate the heavy lifting (image delivery via CDN) from application logic (Django) and metadata (Postgres).",
      "When sharding databases, embedding the shard ID into the primary key removes the need for centralized routing tables.",
      "Use in-memory data stores like Redis for highly read-heavy, low-latency data structures like user feeds."
    ],
    "furtherReading": [
      {
        "title": "Sharding & IDs at Instagram",
        "url": "https://instagram-engineering.tumblr.com/post/10853187575/sharding-ids-at-instagram"
      }
    ]
  },
  "case-stripe-idempotency-keys": {
    "title": "Case: Stripe's idempotency keys",
    "video": {
      "youtubeId": "olfaBgJrUBI",
      "title": "Design a Payment System - System Design Interview",
      "channel": "Code with Lucian"
    },
    "content": "<div class=\"lesson-content\">\n      <h2>Case: Stripe's Idempotency Keys</h2>\n      <p>An idempotency key tells a server which intended operation a request belongs to. That matters deeply in payment systems: when the server completes work but its reply never reaches the caller (due to a network timeout), another attempt should recover the result without charging the customer twice.</p>\n      \n      <p>Stripe's implementation of idempotency is the industry gold standard for building robust distributed systems where network failures are inevitable.</p>\n      \n      <h3>The Problem: The Two Generals</h3>\n      <p>Imagine a client sends a <code>POST /v1/charges</code> request to Stripe to charge $50. Stripe processes the payment, deducts the money, and sends back an HTTP 200 OK. But right before the response reaches the client, the client's wifi drops.</p>\n      \n      <p>The client's code receives a timeout. The client doesn't know if the request never reached Stripe, or if Stripe processed it but the response was lost. If the client retries the request, the user might be charged another $50. If the client doesn't retry, the user might not get their goods, even though they were charged.</p>\n      \n      <h3>The Solution: Idempotency Keys</h3>\n      <p>Stripe supports client-supplied <code>Idempotency-Key</code> headers for eligible mutating requests. One key names one intended operation, so a retry must reuse both the key and the same parameters; a new checkout attempt needs a new key.</p>\n      \n      <pre><code>POST /v1/charges\nIdempotency-Key: a1b2c3d4-e5f6\nAmount: 5000</code></pre>\n      \n      <p>When Stripe receives a request, it checks its database (or a fast distributed cache) to see if it has seen this key before.</p>\n      \n      <ol>\n        <li><strong>First Request:</strong> The service validates and begins one operation under the key. It retains the resulting status and response according to the provider's documented idempotency policy.</li>\n        <li><strong>Network Fails:</strong> The client does not get the response and retries with the <em>same</em> key and parameters.</li>\n        <li><strong>Second Request:</strong> If the first operation has a retained result, the service returns it instead of starting another charge. A concurrent in-progress request or changed parameters must be handled explicitly rather than treated as an ordinary replay.</li>\n      </ol>\n      \n      <h3>Where to Store the Keys</h3>\n      <p>An API can atomically claim an idempotency key with its local operation record and reject the same key with changed parameters. It cannot place an external card-network effect in that database transaction. After a timeout, the provider may have accepted the charge even though the local response record is incomplete, so recovery needs the same downstream operation identity, an explicit unknown state, provider lookup or webhook evidence, and reconciliation.</p>\n      \n      <div class=\"mermaid\">\nsequenceDiagram\n  participant Client\n  participant StripeAPI\n  participant DB\n  \n  Client->>StripeAPI: POST /charges (Key: X)\n  StripeAPI->>DB: Check Key X\n  DB-->>StripeAPI: Not Found\n  StripeAPI->>DB: Process Payment & Save Key X + Response\n  StripeAPI--xClient: (Network Drop) HTTP 200\n  \n  Client->>StripeAPI: Retry POST /charges (Key: X)\n  StripeAPI->>DB: Check Key X\n  DB-->>StripeAPI: Found! Return saved response\n  StripeAPI-->>Client: HTTP 200 (Recovered)\n      </div>\n      \n      <p>Keys are typically expired after 24 hours to save storage space, as retries usually happen within minutes, not days.</p>\n    </div>",
    "keyTakeaways": [
      "Network timeouts are ambiguous: you don't know if the request failed or the response failed.",
      "APIs that mutate state (payments, creates, deletes) must be idempotent to allow safe retries.",
      "Clients generate a unique UUID and pass it in the Idempotency-Key header.",
      "The server caches the resulting HTTP response against the idempotency key and replays it on retries.",
      "Claim the key atomically with local mutation state; external effects still require provider idempotency, unknown-outcome recovery, and reconciliation."
    ],
    "furtherReading": [
      {
        "title": "Stripe Blog: Designing robust and predictable APIs with idempotency",
        "url": "https://stripe.com/blog/idempotency"
      }
    ]
  },
  "case-discord-message-storage": {
    "title": "Case: how Discord stores trillions of messages",
    "video": {
      "youtubeId": "jo6U429l3JM",
      "title": "1 TRILLION messages #javascript #python #web #coding #programming",
      "channel": "ByteByteGo"
    },
    "content": "<div class=\"lesson-content\">\n      <h2>Case: How Discord Stores Trillions of Messages</h2>\n      <p>A message store has to serve both a crowded channel with thousands of active chatters and a quiet direct message conversation whose latest messages are months old. Discord's database history shows why those two reads stress different parts of the same system.</p>\n      \n      <p>Discord started with MongoDB, migrated to Cassandra to handle billions of messages, and more recently migrated to ScyllaDB to handle trillions. The evolution is a masterclass in understanding database architectures.</p>\n      \n      <h3>The Migration to Cassandra</h3>\n      <p>In 2017, Discord moved from MongoDB to Cassandra. They needed a database that provided linear scalability, high availability, and fast writes. Cassandra, a wide-column NoSQL store, fit the bill perfectly because of its LSM-tree architecture, which makes writes incredibly fast.</p>\n      \n      <p>Discord's published Cassandra design bounded partitions with a composite partition key such as <code>(channel_id, bucket)</code> and ordered messages by a timestamp-bearing <code>message_id</code>. Replication placed each partition on multiple nodes; bucketing limited partition size but did not spread one currently hot bucket across many write owners.</p>\n      \n      <div class=\"mermaid\">\nflowchart TD\n  App[\"Discord API\"] --> Ring[\"Cassandra Ring\"]\n  Ring --> Node1[\"Node A\"]\n  Ring --> Node2[\"Node B\"]\n  Ring --> Node3[\"Node C\"]\n  \n  Node1 -.->|channel_1 data| Disk1[(\"SSTables\")]\n      </div>\n      \n      <h3>Partition Size and Hot Buckets</h3>\n      <p>Time buckets keep a channel's lifetime history from becoming one unbounded partition and let old buckets age independently. They do not solve every hot-key problem: all writes for one channel's current bucket still target that partition's replica set. A sudden viral channel can therefore overload its current owners until the workload is further sharded, buffered, or moved.</p>\n      \n      <h3>Read Performance and Garbage Collection</h3>\n      <p>Cassandra is written in Java. As Discord scaled to trillions of messages, the Java Garbage Collector (GC) became a massive bottleneck. When GC paused the application to clean up memory, latency spiked across the cluster, causing cascading timeouts.</p>\n      \n      <p>Furthermore, reading old messages required waking up disks and reading multiple SSTables (immutable data files on disk). To mitigate this, Discord built a Rust-based data service in front of the database to aggressively cache hot data and collapse multiple simultaneous queries for the same channel into a single query.</p>\n      \n      <h3>The Move to ScyllaDB</h3>\n      <p>Ultimately, Discord migrated from Cassandra to ScyllaDB. ScyllaDB's C++ shard-per-core architecture avoids JVM garbage-collection pauses and offers substantial CQL compatibility, but predictable latency still depends on compaction, disks, caching, repair, and data modeling. Discord also changed service and query behavior during the migration; compatibility did not make the systems operationally identical.</p>\n    </div>",
    "keyTakeaways": [
      "Wide-column stores (Cassandra/Scylla) excel at high-throughput write workloads due to LSM trees.",
      "Partitioning by channel keeps related data together but creates Hot Partitions for viral channels.",
      "Time buckets bound partition size and distribute historical buckets over time, but the current bucket can remain hot.",
      "Runtime and storage-engine behavior matter at extreme scale; avoiding JVM GC was one benefit of Discord's move to ScyllaDB.",
      "Coalescing concurrent reads at the application layer protects the database from thundering herds."
    ],
    "furtherReading": [
      {
        "title": "How Discord Stores Trillions of Messages",
        "url": "https://discord.com/blog/how-discord-stores-trillions-of-messages"
      }
    ]
  },
  "case-amazon-dynamo": {
    "title": "Case: Amazon Dynamo, the paper",
    "video": {
      "youtubeId": "ziq7FUKpCS8",
      "title": "Cassandra in 100 Seconds",
      "channel": "Fireship"
    },
    "content": "<div class=\"lesson-content\">\n      <h2>Case: Amazon Dynamo (The 2007 Paper)</h2>\n      <p>Dynamo was Amazon's internal key-value store designed for services that absolutely needed to keep accepting useful work during failures. Its 2007 paper is a foundational text in distributed systems, heavily influencing the creation of Cassandra, Riak, and modern DynamoDB.</p>\n      \n      <p>Amazon's shopping-cart service prioritized accepting add-to-cart operations through many node and network failures and reconciling divergent versions later. This was an availability goal within reachable-capacity limits, not a promise that every request can succeed under every partition.</p>\n      \n      <h3>Always Writable</h3>\n      <p>A leader-based database may pause writes while establishing a new leader, depending on its replication and failover policy; this is not an inherent property of all relational databases. Dynamo instead chose a leaderless design that favors accepting operations during many partitions and resolving concurrent versions later.</p>\n      \n      <p>Dynamo is a leaderless, multi-master system. Any node can accept a write. But what happens if two users (or one user on two different devices during a network split) update the same cart at the same time on different nodes?</p>\n      \n      <h3>Vector Clocks and Conflict Resolution</h3>\n      <p>Because multiple nodes can accept writes independently, conflicting versions of the data will emerge. Dynamo does not try to resolve these conflicts at write time—that would block the write. Instead, it pushes the conflict resolution to read time.</p>\n      \n      <p>To track versions, Dynamo uses <strong>Vector Clocks</strong>. A vector clock is a list of (node, counter) pairs. When a client reads a shopping cart, it might get back two conflicting versions along with their vector clocks. The client application (the shopping cart service) is responsible for merging them (e.g., combining the items from both versions) and writing the merged result back to Dynamo.</p>\n      \n      <div class=\"mermaid\">\nflowchart TD\n  Client[\"Client\"] --> NodeA[\"Node A\"]\n  Client --> NodeB[\"Node B\"]\n  NodeA -- \"Writes V1\" --> Client\n  NodeB -- \"Writes V2\" --> Client\n  Client -. \"Client merges V1 & V2\" .-> Merged[\"Merged V3\"]\n  Merged --> NodeA\n      </div>\n      \n      <h3>Consistent Hashing</h3>\n      <p>To distribute data across nodes, Dynamo uses <strong>Consistent Hashing</strong>. The nodes are arranged in a logical ring. A key is hashed and placed on the ring, and it is stored on the first node it encounters moving clockwise.</p>\n      <p>This ensures that if a node is added or removed from the cluster, only the data belonging to its immediate neighbors needs to be remapped, rather than remapping the entire database (which would happen with simple modulo hashing).</p>\n      \n      <h3>Tunable Consistency (N, R, W)</h3>\n      <p>Dynamo introduced the concept of tunable consistency using three variables:</p>\n      <ul>\n        <li><strong>N:</strong> The number of nodes that store replicas of the data (e.g., 3).</li>\n        <li><strong>W:</strong> The number of nodes that must acknowledge a write for it to be considered successful (e.g., 2).</li>\n        <li><strong>R:</strong> The number of nodes that must respond to a read request (e.g., 2).</li>\n      </ul>\n      <p>Under a fixed replica set, a successful write to W replicas and a read from R replicas with <code>W + R > N</code> forces at least one overlap. That does not by itself guarantee one unambiguous latest value under concurrent writes, node failures, or Dynamo's sloppy quorum, where fallback nodes may satisfy requests outside the original preference list. Reads must reconcile returned causal versions, and lower W generally increases the chance of stale or missing observations.</p>\n    </div>",
    "keyTakeaways": [
      "Leaderless replication can preserve writes through some node and network failures when enough reachable replicas or fallback nodes remain; it cannot promise availability under every partition.",
      "Conflict resolution can be delayed until read time, pushing the business logic to the application layer.",
      "Vector Clocks track the causal history of updates to identify and resolve concurrent modifications.",
      "Consistent Hashing minimizes data movement when scaling the database cluster up or down.",
      "Quorum protocols (N, R, W) allow engineers to tune the tradeoff between latency, consistency, and availability."
    ],
    "furtherReading": [
      {
        "title": "Dynamo: Amazon's Highly Available Key-value Store",
        "url": "https://www.allthingsdistributed.com/files/amazon-dynamo-sosp2007.pdf"
      }
    ]
  },
  "case-gitlab-database-incident": {
    "title": "Case: GitLab's database incident",
    "video": {
      "youtubeId": "u4EgIU8_f5U",
      "title": "Multi-active disaster recovery in CockroachDB",
      "channel": "CockroachDB"
    },
    "content": "<div class=\"lesson-content\">\n      <h2>Case: GitLab's Database Incident</h2>\n      <p>On January 31, 2017, GitLab suffered a major incident that resulted in the deletion of production database data. Service returned the next day using an older copy, leaving about six hours of database changes unrecovered. The postmortem is a vital lesson in operations, fatigue, and backup strategies.</p>\n      \n      <p>The incident wasn't caused by a malicious hacker or a complex software bug; it was a cascading series of operational failures, exacerbated by human fatigue, culminating in an engineer accidentally running <code>rm -rf</code> on the primary production database instead of a misbehaving replica.</p>\n      \n      <h3>The Anatomy of the Outage</h3>\n      <p>GitLab was experiencing high load caused by spam accounts, causing the PostgreSQL primary database to struggle. A secondary replication node fell far behind. An engineer decided to wipe the lagging secondary node and restart the replication process from scratch.</p>\n      \n      <p>The engineer opened multiple terminal windows. Due to exhaustion (it was late at night) and identical terminal prompts, the engineer typed <code>rm -rf /var/opt/gitlab/postgresql/data</code> in the terminal connected to the <strong>primary</strong> production node, not the secondary node.</p>\n      \n      <p>They realized the mistake seconds later and hit Ctrl+C. The production directory was roughly 300GB, but the incident timeline reports that about 4.5GB had been removed before interruption; the logical loss was larger because recovery ultimately used an older copy.</p>\n      \n      <h3>The Backup Failures</h3>\n      <p>Accidental deletions happen. This is why we have backups. But when GitLab tried to restore from backups, they discovered a catastrophic truth: none of their five backup mechanisms worked.</p>\n      \n      <div class=\"mermaid\">\nflowchart TD\n  Disaster[\"Production Data Deleted\"] --> B1{\"LVM Snapshots?\"}\n  B1 -- No --> B2{\"Regular Backups?\"}\n  B2 -- Failed --> B3{\"Disk Snapshots?\"}\n  B3 -- Not Enabled --> B4{\"S3 Backups?\"}\n  B4 -- Empty --> B5{\"Staging Sync?\"}\n  B5 -- Out of Date --> Recovery[\"6 Hours Data Lost\"]\n      </div>\n      \n      <ul>\n        <li><strong>Automated pg_dump:</strong> The cron job was failing silently because it was using an incompatible version of pg_dump.</li>\n        <li><strong>Azure Disk Snapshots:</strong> These were not enabled for the database servers.</li>\n        <li><strong>LVM Snapshots:</strong> The process to take LVM snapshots was broken and silently ignored.</li>\n        <li><strong>S3 Backups:</strong> The S3 bucket where backups were supposedly being sent was empty.</li>\n      </ul>\n      \n      <p>They eventually restored the database using an LVM snapshot taken on a staging server six hours prior, which happened to be syncing from production. Six hours of user data (issues, merge requests, comments) was permanently lost.</p>\n      \n      <h3>The Lessons Learned</h3>\n      <p>The incident highlighted the stark difference between \"having backups\" and \"having a tested recovery process\". Data doesn't exist unless it has been successfully restored.</p>\n      \n      <p>It also brought attention to operability. Production systems must have safeguards against human error, visual indicators of which environment a terminal is connected to, and blameless postmortem cultures. GitLab famously live-streamed their entire recovery process on YouTube, setting a new standard for transparency in the industry.</p>\n    </div>",
    "keyTakeaways": [
      "A backup system is useless if you have not routinely tested the restoration process.",
      "Monitor your backup jobs just as rigorously as you monitor your production application.",
      "Human error under fatigue is inevitable; design systems with 'undo' buttons, delayed deletions, or strict blast-radius controls.",
      "Use visual cues (different colored terminal prompts) to distinguish production servers from staging or replicas.",
      "Blameless postmortems and transparency build trust faster than hiding mistakes."
    ],
    "furtherReading": [
      {
        "title": "GitLab Postmortem of database outage",
        "url": "https://about.gitlab.com/blog/postmortem-of-database-outage-of-january-31/"
      }
    ]
  }
};
